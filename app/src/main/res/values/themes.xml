<!-- res/values/themes.xml -->
<resources>
    <!--
        Базовая тема — NoActionBar + Dark.
        Compose управляет всем UI, системный тема только для статус-бара.
    -->
    <style name="Base.Theme.Soyle" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">#FF000000</item>
        <item name="android:navigationBarColor">#FF000000</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.Soyle" parent="Base.Theme.Soyle" />

    <!-- Splash Screen (API 12+) -->
    <style name="Theme.Soyle.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#000000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Soyle</item>
    </style>
</resources>